sap.ui.define([],

    function(){
        "use strict";
        
        return {
            toUpper: function(sValue){
                return sValue && sValue.toUpperCase();
            }
        };
    }
);